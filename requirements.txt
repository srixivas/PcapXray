# PcapXray dependencies — Python 3.10+

# PCAP parsing engines
scapy>=2.5.0
pyshark>=0.6

# IP/network utilities
ipwhois>=1.2.0
netaddr>=0.9.0

# Image processing (Resampling.LANCZOS requires >=10.0.0)
pillow>=10.0.0

# Tor consensus
stem>=1.8.2

# Graph rendering
networkx>=3.0
graphviz>=0.20

# Interactive visualization
pyvis>=0.3.2

# CEF embedded browser (macOS broken — being replaced by pywebview in Phase 4)
cefpython3

# TLS cipher suite parsing
cryptography>=41.0.0
